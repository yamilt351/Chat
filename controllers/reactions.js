export const sendReactions = async (req, res, next) => {
		try {
			console.log('send reactions');
		} catch (error) {
			next(error)
		}
}
export const deleteReactions = async (req, res, next) => {
		try {
			console.log('delete reactions');
		} catch (error) {
			next(error)
		}
}
export const editReactions = async (req, res, next) => {
		try {
			console.log('edit reactions');
		} catch (error) {
			next(error)
		}
}
export const getReactionsById = async (req, res, next) => {
		try {
			console.log(' reactions by id');
		} catch (error) {
			next(error)
		}
}
export const allReactions = async (req, res, next) => {
		try {
			console.log('all reactions');
		} catch (error) {
			next(error)
		}
}
export const searchReactions = async (req, res, next) => {
		try {
			console.log('search reactions');
		} catch (error) {
			next(error)
		}
}
